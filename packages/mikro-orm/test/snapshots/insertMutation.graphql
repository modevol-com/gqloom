type Mutation {
  insertMutation(data: UserRequiredInput!): User!
}

type User {
  id: ID!
  name: String!
  email: String!
  age: Int
}

input UserRequiredInput {
  id: ID
  name: String!
  email: String!
  age: Int
  posts: [ID]
}